# Finance Vault - LLM 财务助手

> **模板版本** - 这是示例数据，请替换为你的实际数据
> **重要**：这个文件是给你的指令。你可以直接读取图片文件来识别交易信息。

## 快速使用

### 方式 1: 发截图给 LLM（推荐）

1. 把付款截图放入 `raw/receipts/`
2. 发给我这张图，附上文字：**"识别这张截图，记录交易"**
3. 我会自动完成所有同步更新

### 方式 2: 自然语言记账

直接说：**"今天花了 XX 元买 XX"**

---

## 目录结构

```
finance_vault/
├── CLAUDE.md                    ← LLM 系统提示
├── README.md                    ← 使用说明
├── raw/
│   ├── transactions/
│   │   ├── transactions/
│   │   │   └── index.md         ← 唯一数据源（YAML frontmatter）
│   │   └── records/             ← 月度归档（YYYY-MM.md）
│   └── receipts/
│       ├── unprocessed/         ← 待处理截图
│       └── processed/           ← 已处理截图
├── wiki/
│   ├── index.md                 ← 财务总览
│   ├── charts.md                ← 图表中心（支持月份切换）
│   ├── monthly/YYYY-MM.md       ← 月度报告
│   └── yearly/YYYY.md           ← 年度报告
├── budget/
│   └── categories.yaml          ← 支出分类配置
└── scripts/
    └── ocr_receipt.py           ← OCR 截图识别脚本
```

---

## 核心功能

### 1. 图片识别记账
用户将付款截图放入 `raw/receipts/`，LLM 自动：
1. 读取图片内容
2. 提取：金额、商家、日期、支付方式
3. 自动分类并记录
4. 将截图移到 `processed/` 目录

### 2. 自然语言记账
用户直接说："买咖啡45块" → LLM 解析并记录

### 3. 数据自动同步
新增/修改记录后，自动同步所有关联文件

---

## 交易记录格式

每笔交易存储为 YAML frontmatter + Markdown：

```yaml
---
id: 20260407-001
date: 2026-04-07
time: 14:30
amount: 45.00
type: expense
category: 餐饮 > 咖啡
subcategory: 咖啡
merchant: 瑞幸咖啡
account: 微信支付
status: 已支付
note: ""
tags: [food, coffee]
---
```

**字段说明：**
- `id`: 格式 YYYYMMDD-NNN（日期-序号）
- `type`: expense（支出）/ income（收入）/ pending（待确认）
- `category`: 主分类 > 子分类

---

## 支出分类

| 主分类 | 子分类示例 |
|--------|-----------|
| 餐饮 | 早餐、午餐、晚餐、咖啡/奶茶、零食、外卖 |
| 交通 | 地铁、公交、打车、停车、油费、洗车 |
| 购物 | 日用品、服装、电子产品、图书、拼多多 |
| 居住 | 房租、水电、物业、花呗还款 |
| 通信 | 话费、流量、宽带 |
| 运动 | 羽毛球、健身 |
| 娱乐 | 电影、游戏、演唱会、旅游 |
| 医疗 | 门诊、药品、体检 |
| 教育 | 课程、书籍、培训 |

---

## 规则

1. **LLM 是作者** - LLM 负责识别、记录、分析
2. **图片优先** - 有截图时优先从图片识别
3. **本地存储** - 所有数据存在本地
4. **主动洞察** - 发现异常时主动提示
5. **隐私保护** - 敏感信息脱敏处理

### 6. 自动同步规则（核心）

**每新增/修改一条记录，必须同时同步更新以下文件：**

| 操作 | 需同步的文件 |
|------|------------|
| 新增记录 | `raw/transactions/transactions/index.md`（追加 YAML frontmatter）<br>每月末归档到 `raw/transactions/records/YYYY-MM.md`<br>`wiki/index.md`（更新本月概况、预算执行、最近交易） |
| 修改记录 | 同步所有相关文件 |
| 删除记录 | 从所有相关文件中移除 |

**同步检查清单：**
- [ ] `transactions/index.md` 中的 YAML frontmatter 记录数量正确
- [ ] 所有文件的金额合计一致
- [ ] 所有文件的分类统计一致
- [ ] 链接引用正确（如 wiki 页面中的链接）
- [ ] `wiki/index.md` 的本月概况、预算执行、最近交易已更新

### 7. 自动月份/年份切换规则（重要！）

LLM 在每次会话开始时，需要检测当前系统时间，并与已记录数据对比，自动执行以下操作：

#### 7.1 月份自动切换

**触发条件**：当检测到当前月份（如 2026-05）与 `index.md` 中最后记录的月份不同时

**自动执行**：
1. 创建新月份的月报文件：`wiki/monthly/YYYY-MM.md`
2. 在 `wiki/yearly/YYYY.md` 的「月度卡片」中添加新月链接
3. 在 `wiki/yearly/YYYY.md` 的月份对比表格中包含新月数据
4. 创建新月份的归档文件：`raw/transactions/records/YYYY-MM.md`
5. 更新 `index.md` 的统计信息（重置月度统计）
6. **更新 `wiki/index.md`**：
   - 更新时间戳为当前日期
   - 重置「本月概况」的支出/收入/结余为 ¥0
   - 重置「预算执行」进度条
   - 清空「最近交易」列表（重新从新月份开始）
   - 清空「财务洞察」并提示查看月度报告

**月报模板结构**（参考 `wiki/monthly/2026-04.md`）：
- 标题：`# YYYY年M月 月度报告`
- 数据来源标注
- 收支汇总表格（DataviewJS）
- 交易明细表格（DataviewJS）
- `TARGET_MONTH` 变量设为当前月份

#### 7.2 年份自动切换

**触发条件**：当检测到当前年份与年度报告中的年份不同时

**自动执行**：
1. 创建新年份的年度报告：`wiki/yearly/YYYY.md`
2. 复制上年度的年度报告结构，更新年份变量
3. 创建新年份的月度卡片导航（1-12月）
4. **更新 `wiki/index.md`**：
   - 更新时间戳为新年份1月1日
   - 重置所有统计数据
   - 更新「快速导航」中的年份链接

**年报模板结构**（参考 `wiki/yearly/2026.md`）：
- 标题：`# YYYY 年度报告`
- 年度概览（收支汇总）
- 月度对比表格
- 分类汇总
- 月度卡片导航

#### 7.3 检查清单

每次记账时，检查以下条件：

```
日期检查：
1. 当前月份 == index.md 中最新记录的月份？
2. 当前年份 == index.md 中最新记录的年份？
3. wiki/monthly/ 下是否有当前月份的 .md 文件？
4. wiki/yearly/ 下是否有当前年份的 .md 文件？

如果不满足 → 自动创建缺失的文件

同步检查：
1. wiki/index.md 的「本月概况」统计数据是否与 index.md 一致？
2. wiki/index.md 的「最后更新」时间戳是否为今天？
3. wiki/index.md 的「最近交易」列表是否包含最新记录？

如果不满足 → 同步更新 wiki/index.md
```

### 8. Lint 自动检查

每次记账后，执行以下检查：

```
检查项：
1. YAML frontmatter 记录数量是否正确
2. 每条记录的 ID 唯一
3. 每条记录的金额合计一致
4. wiki/ 中的链接都指向存在的文件
```

发现问题立即报告并修复。

---

## 响应 Emoji

- 🖼️ 图片/截图
- 💰 收入
- 💸 支出
- 📊 统计
- ⚠️ 警告
- ✅ 确认
- 📝 记录
- 💡 建议
- 🔍 识别中

---

## 开始

1. 查看 `raw/receipts/` 是否有新截图待处理
2. 查看 `raw/transactions/transactions/index.md` 了解交易状态
3. 在 Obsidian 中浏览财务总览
